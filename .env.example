# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGO_URI=mongodb+srv://dbuser:password@cluster0.ler3cub.mongodb.net/student_enrollment?retryWrites=true&w=majority&appName=Cluster0

# Flask Secret Key (Used for session encryption)
# Generate a secure key: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: Change this value in production!
SECRET_KEY=your-secret-key-change-in-production

# Environment (development or production)
# Set to 'production' when deploying to Render
ENVIRONMENT=development
